name: Second Workflow

on:
  repository_dispatch:
    types:
      - second-workflow-triggered

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Add your desired steps for the second workflow
